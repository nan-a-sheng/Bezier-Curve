# 计算机图形学实验三：交互式贝塞尔曲线绘制（Taichi + Python）

## 一、实验目标
1. 理解贝塞尔曲线（Bézier Curve）的几何意义与控制点作用。
2. 掌握并编码实现求取贝塞尔曲线的 De Casteljau 递归插值算法。
3. 理解光栅化、帧缓存（Frame Buffer）、像素寻址与颜色赋值底层原理。
4. 掌握图形界面鼠标点击添加控制点、键盘清空画布的交互事件处理。
5. 遵循现代高性能渲染规范：CPU批量计算 + GPU并行绘制，减少跨设备数据传输开销。

## 二、数学原理：De Casteljau 算法
贝塞尔曲线由一组控制点 \(P_0,P_1,\dots,P_{n-1}\) 定义，引入参数 \(t\in[0,1]\)，t 从 0 变化到 1 即可遍历整条曲线。

算法核心流程：
1. 给定一组原始控制点与参数 \(t\)；
2. 相邻两点做线性插值：  
\[P'_i=(1-t)\cdot P_i+t\cdot P_{i+1}\]
得到数量少 1 的新点集；
3. 对上一轮生成的新点集重复插值，不断降阶；
4. 最终仅剩一个点，即为当前 \(t\) 对应贝塞尔曲线上精确坐标。

该算法数值稳定性高，是工程上生成贝塞尔曲线的标准实现方式。

## 三、光栅化基础原理
1. 程序开辟 **800×800 像素缓冲区（Taichi Field）**，模拟显存帧缓存；
2. De Casteljau 输出为 \([0,1]\) 归一化浮点坐标；
3. 将浮点坐标 × 屏幕分辨率，映射为整型像素索引；
4. 对合法坐标（边界校验）直接修改像素 RGB 值，完成“点亮像素”光栅化操作。

## 四、实验环境
- 编程语言：Python 3.12
- 计算框架：Taichi 1.7.4（GPU加速）
- 运行环境：Conda 虚拟环境 `cg_env`
- 窗口分辨率：800 × 800
- 曲线采样分段：1000 段
- 最大控制点容量：100（固定显存池）

## 五.程序操作说明
操作	        功能
鼠标左键单击	在点击位置新增贝塞尔控制点
按住左键连续点击	生成高阶贝塞尔曲线，实时观察曲线形变
键盘按下 C	清空所有控制点，重置空白画布
实时渲染	自动绘制灰色控制多边形 + 绿色贝塞尔拟合曲线

## 六.高性能渲染设计要点
1.CPU 只负责批量算出全部曲线点，不逐点与 GPU 通信；
2.全部像素点亮、画线逻辑放到 GPU Kernel 并行执行；
3.全程使用预先开辟的固定显存池，不动态申请、释放内存；
4.所有数据采用一次性批量传输（Batch），大幅降低 CPU–PCIe–GPU 开销。

## 七.项目目录结构
Experiment3-Bezier/
├── curve.py        # 主程序完整代码
├── README.md       # 项目说明文档（本文档）
├── imgui.ini       # GUI窗口自动配置文件

## 八.实验总结
本次实验完整实现了基于 Taichi 的交互式贝塞尔曲线绘制系统：
1.深入理解并落地 De Casteljau 插值原理；
2.掌握光栅化、帧缓存、像素寻址的底层图形学逻辑；
3.熟练实现鼠标、键盘交互事件与实时图形刷新；
4.严格遵循高性能渲染规范，理解 CPU 预处理 + GPU 并行绘制的工业级思路；
5.通过拓展内容，对比理解贝塞尔曲线与 B 样条曲线差异，加深自由曲线建模认知。

## 九.实验结果展示
![kbrbjHPs_converted](https://github.com/user-attachments/assets/8577b340-46db-4bf3-b352-d1149a193c68)
